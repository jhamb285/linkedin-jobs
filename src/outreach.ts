/**
 * REMOVED in Phase 6.
 *
 * The auto-poster (PhantomBuster Bereach + DM sender) lived here in the
 * SQLite era. Per LEAD_MAGNET_AUDIT.md decision #6, the new platform is
 * human-in-the-loop only — Srikant ships drafts via the Today UI. This
 * file is intentionally empty so existing imports don't break.
 */

export {};
